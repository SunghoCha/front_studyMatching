export default {
    newEvents(state) {
        return state.newEvents
    },
    oldEvents(state) {
        return state.oldEvents
    },
    currentEnrollment(state) {
        return state.currentEnrollment;
    },
}